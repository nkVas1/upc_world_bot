# 🚀 Railway.app - Полный гайд развёртывания

## 📋 Содержание

1. [Предварительные требования](#предварительные-требования)
2. [Пошаговое развёртывание](#пошаговое-развёртывание)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Проверка и мониторинг](#проверка-и-мониторинг)
5. [Решение проблем](#решение-проблем)

---

## 🔧 Предварительные требования

### Что нужно подготовить:

1. **GitHub аккаунт** и репозиторий с этим проектом
2. **Railway аккаунт** (бесплатно на [railway.app](https://railway.app))
3. **Telegram BOT_TOKEN** от @BotFather
4. **Telegram ADMIN_IDS** - ваш ID (от @userinfobot)
5. **PostgreSQL URL** (можно использовать Railway PostgreSQL плагин)
6. **Redis URL** (можно использовать Railway Redis плагин)

---

## 🚀 Пошаговое развёртывание

### Шаг 1: Коммитьте и запушьте код на GitHub

```bash
cd G:\CODING\UP_BOT_NEW\upc_world_bot

# Убедитесь что все файлы в git
git status

# Добавьте все изменения
git add -A

# Закоммитьте с понятным сообщением
git commit -m "Optimize: Make config Railway-compatible with default values"

# Запушьте на GitHub
git push origin main
```

### Шаг 2: Создайте проект в Railway

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub"**
4. Подключите ваш GitHub аккаунт (если ещё не подключен)
5. Выберите репозиторий `UP_BOT_NEW`
6. Выберите ветку `main`
7. Нажмите **"Deploy"**

Railway автоматически обнаружит `Dockerfile` и начнёт сборку.

### Шаг 3: Добавьте PostgreSQL

1. В вашем Railway проекте нажмите **"+ New"**
2. Выберите **"Database"**
3. Выберите **"PostgreSQL"**
4. Railway создаст базу данных

✅ `DATABASE_URL` будет автоматически добавлен в переменные окружения

### Шаг 4: Добавьте Redis

1. Нажмите **"+ New"**
2. Выберите **"Database"**
3. Выберите **"Redis"**
4. Railway создаст Redis сервис

✅ `REDIS_URL` будет автоматически добавлен в переменные окружения

### Шаг 5: Настройте переменные окружения

1. Откройте **Variables** в своем Railway проекте
2. Добавьте **обязательные** переменные:

```
BOT_TOKEN=<ваш токен от @BotFather>
BOT_USERNAME=UPCworld_bot
ADMIN_IDS=<ваш Telegram ID>
```

3. **Опционально:** добавьте production значения для security переменных:

```
SECRET_KEY=<сгенерировать случайную строку>
JWT_SECRET=<сгенерировать случайную строку>
ENCRYPTION_KEY=<ровно 32 символа>
```

Остальные переменные уже имеют разумные дефолтные значения.

---

## 🔐 Настройка переменных окружения

### Обязательные переменные (MUST HAVE):

| Переменная | Значение | Где получить |
|-----------|----------|--------------|
| `BOT_TOKEN` | `8446133461:AAEmeqQXUjNjzwgSLqJX2uOf_lVLgN2-VIw` | @BotFather в Telegram |
| `BOT_USERNAME` | `UPCworld_bot` | То же имя, что в @BotFather |
| `DATABASE_URL` | `postgresql+asyncpg://...` | Railway PostgreSQL плагин |
| `REDIS_URL` | `redis://...` | Railway Redis плагин |

Railway **автоматически** добавит `DATABASE_URL` и `REDIS_URL` когда вы создадите PostgreSQL и Redis.

### Рекомендуемые переменные (HIGHLY RECOMMENDED):

| Переменная | Генерация | Заметки |
|-----------|----------|---------|
| `ADMIN_IDS` | Ваш Telegram ID от @userinfobot | Для доступа к админ-панели |
| `SECRET_KEY` | `python -c "import secrets; print(secrets.token_urlsafe(32))"` | Min 32 символа |
| `JWT_SECRET` | `python -c "import secrets; print(secrets.token_urlsafe(32))"` | Min 32 символа |

### Опциональные переменные (NICE TO HAVE):

Эти уже имеют дефолтные значения и менять необязательно:

- `WEBSITE_URL` - по умолчанию `https://under-people-club.vercel.app`
- `WEBSITE_API_KEY` - дефолт для разработки
- `ENCRYPTION_KEY` - дефолт `12345678901234567890123456789012`
- И другие...

### Как добавить переменные в Railway:

1. Откройте ваш Railway проект
2. Кликните на сервис (бот или PostgreSQL)
3. Откройте вкладку **"Variables"**
4. Нажмите **"+ Add Variable"**
5. Введите имя и значение
6. Нажмите Enter или кнопку добавления

---

## ✅ Проверка и мониторинг

### Посмотреть логи:

1. Откройте Railway dashboard
2. Выберите ваш проект
3. Кликните на сервис (бот)
4. Откройте вкладку **"Logs"**

### Успешный запуск выглядит так:

```log
[inf] Starting Container
[inf] Railway CLI loaded
[inf] Building image...
[inf] Container started
{"event": "starting_upc_world_bot", "level": "info"}
{"event": "database_initialized", "level": "info", "url": "postgresql+asyncpg://..."}
{"event": "bot_started", "level": "info", "mode": "polling"}
```

### Ошибка конфигурации выглядит так:

```log
ValidationError: 14 validation errors
BOT_TOKEN
  Field required [type=missing]
```

Это означает, что нужно добавить недостающие переменные.

### Проверить, работает ли бот:

1. Откройте Telegram
2. Найдите вашего бота (например, @UPCworld_bot)
3. Отправьте команду `/start`
4. Если бот ответил - успешно! ✅

---

## 🔍 Решение проблем

### Проблема 1: "Build failed - No Dockerfile found"

**Решение:** Убедитесь что в корне проекта есть файл `Dockerfile` и запушьте его:

```bash
git add Dockerfile
git commit -m "Add Dockerfile for Railway deployment"
git push
```

---

### Проблема 2: "ValidationError: BOT_TOKEN Field required"

**Решение:**
1. Откройте Variables в Railway
2. Убедитесь что добавлены переменные:
   - `BOT_TOKEN`
   - `BOT_USERNAME`
   - `DATABASE_URL`
   - `REDIS_URL`
3. Переразверните (нажмите **Redeploy**)

---

### Проблема 3: "connection was closed in the middle of operation"

**Решение:** Проблема с подключением к БД:
1. Проверьте что PostgreSQL плагин создан в Railway
2. Убедитесь что `DATABASE_URL` скопирован правильно
3. Проверьте что БД инициализирована (может потребоваться время)

---

### Проблема 4: "No module named 'telegram'"

**Решение:** Зависимости не установились:
1. Проверьте что `requirements.txt` в корне проекта
2. Запушьте его на GitHub
3. Переразверните проект

---

### Проблема 5: Бот запускается, но не отвечает на сообщения

**Решение:**
1. Проверьте что `BOT_TOKEN` правильный (от @BotFather)
2. Проверьте что бот имеет права на обработку сообщений
3. Посмотрите логи для деталей ошибок

---

## 💡 Полезные команды для генерации переменных

### Сгенерировать SECRET_KEY:

```bash
python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
```

### Сгенерировать JWT_SECRET:

```bash
python -c "import secrets; print('JWT_SECRET=' + secrets.token_urlsafe(32))"
```

### Сгенерировать ENCRYPTION_KEY (ровно 32 символа):

```bash
python -c "import secrets; key = secrets.token_urlsafe(24); print(f'ENCRYPTION_KEY={key[:32]}' if len(key) >= 32 else f'ENCRYPTION_KEY={key.ljust(32, \"0\")}')"
```

Или просто используйте дефолт:
```
ENCRYPTION_KEY=12345678901234567890123456789012
```

---

## 🎯 Чек-лист успешного развёртывания

- [ ] Код запушен на GitHub (последний коммит с конфигом)
- [ ] Railway проект создан
- [ ] PostgreSQL плагин добавлен
- [ ] Redis плагин добавлен
- [ ] Переменные `BOT_TOKEN` и `BOT_USERNAME` добавлены
- [ ] Переменные `DATABASE_URL` и `REDIS_URL` видны (добавлены Railway автоматически)
- [ ] Проект успешно развёрнут (статус "Deployed")
- [ ] Логи не показывают ValidationError
- [ ] Бот отвечает на команду `/start` в Telegram

---

## 📞 Дополнительная помощь

Если что-то не работает:

1. **Проверьте логи** - Most problems видны там
2. **Убедитесь в переменных** - Railway не читает `.env` файл!
3. **Перезагрузитесь** - Нажмите Redeploy в Railway
4. **Проверьте GitHub** - Убедитесь что последние изменения запушены

**Удачного развёртывания!** 🚀
